version: 2
models:
  - name: kpi_company_latest
    columns:
      - name: cik
        tests: [not_null]
      - name: period_end_date
        tests: [not_null]
      - name: revenue
        tests: []   # keep soft; not all periods have revenue
  - name: kpi_ttm_revenue
    columns:
      - name: cik
        tests: [not_null]
      - name: period_end_date
        tests: [not_null]
      - name: ttm_revenue
        tests: [not_null]